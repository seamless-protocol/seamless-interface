query CollateralPriceHistorical($address: ID!) {
  leverageToken(id: $address) {
    lendingAdapter {
      oracle {
        decimals
        priceUpdates {
          price
          timestamp
        }
      }
    }
  }
}

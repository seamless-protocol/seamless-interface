export enum QueryTypes {
  // eslint-disable-next-line no-unused-vars
  HOOK = "hook",
  // eslint-disable-next-line no-unused-vars
  CHILD_CHAIN_QUERY = "readContract",
  // eslint-disable-next-line no-unused-vars
  CHILD_API_QUERY = "readApi",
  // eslint-disable-next-line no-unused-vars
  GRAPH_QL_QUERY = "readGraphQL",
}

export const Scopes = {
  morpho: "morpho",
};
